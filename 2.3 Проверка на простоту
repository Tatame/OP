#include <stdio.h>
int main(){
    int n=0, sum=0;
    scanf("%d",&n);
    for(int x=2;x<=n;x++){
        if (n%x==0) sum++;
    }
    if (sum==1) printf("YES");
    else printf("NO");
    return 0;
}
